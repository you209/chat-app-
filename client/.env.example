VITE_QUICK_EXIT_URL=https://www.bom.gov.au

# Required for the native Android build only — the app has no page origin to
# resolve relative API/socket paths against, so this must be an absolute URL
# pointing at your deployed backend. Set this before running `npx cap sync android`.
VITE_API_BASE_URL=https://your-backend.example.com
